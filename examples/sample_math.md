# Sample Math Notes

The original prototype used Word paragraphs prefixed with `latex:` and turned
them into Markdown math blocks.

```text
latex: a^3 - b^3 = (a - b)(a^2 + ab + b^2)
```

Expected Markdown:

$$
a^3 - b^3 = (a - b)(a^2 + ab + b^2)
$$
